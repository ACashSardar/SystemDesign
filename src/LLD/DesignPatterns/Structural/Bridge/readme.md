Video platforms (YouTube, Netflix, Prime) and Video quality processors (HD, 4K, 8K).

This is a classic Bridge use case:
👉 Decouple abstraction (Video) from implementation (VideoProcessor) so both can vary independently.

Primary goal
👉 Avoid class explosion

